{
  "name": "fasterbase-cli",
  "version": "1.0.5",
  "description": "",
  "preferGlobal": true,
  "main": "bin/main.js",
  "bin": {
    "fasterbase-cli": "./bin/main.js"
  },
  "scripts": {
    "local": "rm -rf test && mkdir test && npm r -g . && npm i -g . && fasterbase-cli",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "local": "^0.3.3"
  }
}
